# A11y Authoring Conventions

- Prefer native semantic elements first.
- Use ARIA only when native semantics are insufficient.
- Every form control requires an explicit accessible name.
- Keyboard behavior is required for all interactive controls.
- Focus styles must remain visible in all themes.
