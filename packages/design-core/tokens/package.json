{
  "name": "@ds/tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./tokens.css": "./src/tokens.css"
  },
  "files": [
    "src/tokens.css"
  ],
  "scripts": {
    "build": "mkdir -p dist && cp src/tokens.css dist/tokens.css",
    "typecheck": "echo 'tokens package is css-only'",
    "lint": "echo 'tokens package is css-only'",
    "test": "echo 'tokens package is css-only'"
  }
}
