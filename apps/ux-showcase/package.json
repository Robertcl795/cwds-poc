{
  "name": "@ds/ux-showcase",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@ds/core": "workspace:*",
    "@ds/components": "workspace:*",
    "@ds/tokens": "workspace:*"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4174",
    "test:e2e": "playwright test",
    "test:e2e:visual": "playwright test --grep \"visually stable\"",
    "test:e2e:visual:update": "playwright test --grep \"visually stable\" --update-snapshots"
  }
}
