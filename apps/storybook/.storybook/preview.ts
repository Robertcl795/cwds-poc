import '@ds/tokens/tokens.css';
import '@ds/styles/index.css';

const preview = {
  parameters: {
    a11y: {
      test: 'todo'
    },
    controls: {
      expanded: true
    },
    layout: 'centered'
  },

  tags: ['autodocs']
};

export default preview;
