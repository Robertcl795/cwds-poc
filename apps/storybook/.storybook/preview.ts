import '@covalent-poc/tokens/tokens.css';
import '@covalent-poc/styles/index.css';

const preview = {
  parameters: {
    controls: {
      expanded: true
    },
    layout: 'centered'
  },

  tags: ['autodocs']
};

export default preview;
